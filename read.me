script hist.sh contains main logic of this applications
it creates input/output directories, launches java and R scripts

at first it launches java compiles and launches java class, that cut picture into several chunks to proceed it further.
at second it launches R script, which handles each chunk. this script map every colour, count each entering of this colour, and than reduce 
resulted map. 
resulted values are displayed as plot
